InventoryCreate:
  description: Inventory Creation Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ../schemas/inventory.yaml#/Inventory

InventoryUpdate:
  description: Inventory Update Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ../schemas/inventoryUpdate.yaml#/InventoryUpdate
