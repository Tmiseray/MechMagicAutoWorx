components:
  requestBodies:
    InventoryCreate:
      description: Inventory Creation Request Body
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/inventory.yaml#/components/schemas/Inventory'

    InventoryUpdate:
      description: Inventory Update Request Body
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/inventoryUpdate.yaml#/components/schemas/InventoryUpdate'
