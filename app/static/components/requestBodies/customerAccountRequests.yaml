CustomerAccountCreate:
  description: Customer Account Creation Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/CustomerAccount'

CustomerAccountUpdate:
  description: Customer Account Update Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/CustomerAccountUpdate'

CustomerLogin:
  description: Customer Account Login Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/CustomerLogin'