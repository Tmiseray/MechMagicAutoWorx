CustomerAccountCreate:
  description: Customer Account Creation Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ./components/schemas/customerAccounts.yaml#/CustomerAccount

CustomerAccountUpdate:
  description: Customer Account Update Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ./components/schemas/customerAccountUpdate.yaml#/CustomerAccountUpdate

CustomerLogin:
  description: Customer Account Login Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ./components/schemas/login.yaml#/Login