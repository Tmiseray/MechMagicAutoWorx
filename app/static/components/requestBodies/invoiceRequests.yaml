InvoiceCreate:
  description: Invoice Creation Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ./components/schemas/invoices.yaml#/Invoice

InvoiceUpdate:
  description: Invoice Update Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ./components/schemas/invoiceUpdate.yaml#/InvoiceUpdate
