CustomerCreate:
  description: Customer Information Creation Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ./components/schemas/customers.yaml#/Customer

CustomerUpdate:
  description: Customer Information Update Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ./components/schemas/customerUpdate.yaml#/CustomerUpdate