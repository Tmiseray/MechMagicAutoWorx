components:
  requestBodies:
    CustomerCreate:
      description: Customer Information Creation Request Body
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/customers.yaml#/components/schemas/Customer'

    CustomerUpdate:
      description: Customer Information Update Request Body
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/customerUpdate.yaml#/components/schemas/CustomerUpdate'