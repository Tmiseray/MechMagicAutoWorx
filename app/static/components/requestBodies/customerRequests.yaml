CustomerCreate:
  description: Customer Information Creation Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ../schemas/customers.yaml#/Customer

CustomerUpdate:
  description: Customer Information Update Request Body
  required: true
  content:
    application/json:
      schema:
        $ref: ../schemas/customerUpdate.yaml#/CustomerUpdate