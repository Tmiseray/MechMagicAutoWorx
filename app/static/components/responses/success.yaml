200Login:
  description: Successful login, returns auth token
  content:
    application/json:
      schema:
        type: object
        properties:
          status:
            type: string
          message:
            type: string
          auth_token:
            type: string
      example:
        $ref: ../examples/login.yaml#/LoginSuccess
200AccountDelete:
  description: Successful Account Deletion
  content:
    application/json:
      schema:
        type: object
        properties:
          message:
            type: string
            example: "Account Successfully Deleted"
200CustomerDelete:
  description: Successful Customer Deletion
  content:
    application/json:
      schema:
        type: object
        properties:
          message:
            type: string
            example: "Customer Successfully Deleted"
200MechanicDelete:
  description: Successful Mechanic Deletion
  content:
    application/json:
      schema:
        type: object
        properties:
          message:
            type: string
            example: "Mechanic Successfully Deleted"

