components:
  schemas:
    MechanicLogin:
      type: object
      required:
        - email
        - password
      properties:
        id:
          type: integer
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
